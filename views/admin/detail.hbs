{{#each platos as |plato|}}
<div class="card mb-3" style="max-width: 540px;">
  <div class="row no-gutters">
    <div class="col-md-4">
      <img src="{{plato.image}}" class="card-img" alt="...">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title">{{plato.title}}</h5>
      </div>
    </div>
  </div>
</div>
{{/each}}
        <form action="/admin/pedidos/{{pedido.id}}" method="POST">
          <button type="submit" class="btn btn-primary btn-sm text-white mr-3">
            Done!
          </button>
        </form> 